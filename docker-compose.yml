version: "3.9"

services:
  binacle-pkmngo:
    pull_policy: build
    build:
      context: https://github.com/kermitine/Binacle.git
    container_name: binacle-pkmngo

    ports:
      - "3005:5000"

    restart: unless-stopped
